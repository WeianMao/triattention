以下内容为本项目的历史消息留档（供后续查阅，避免遗漏要求）。

## 用户原始指令（转存）

在我的认知里（你需要确定我的认知是不是对的，错的话请你告诉我）R-KV/weian_script/aime24_official_sampled8/run_fullkv_aime25_official_sampled8_qwen.sh和LazyEviction/weian_script/run_fullkv_sharded_eval.sh这两个脚本应该是高度相近的实验，至少是可比较的实验，模型 benchmark fullkv之类的都是一样的 只有一些不是这么影响的因素 比如采样参数 是否对多个结果的正确率取平均这种理应不会有太大影响的因素是比较不一样的。因此理论上他们的结果应该很接近才对。但是实际上结果区别非常大，比如R-KV下的结果是31.7，但是LazyEviction下的结果是46.6。这就说明我之前的认知可能有问题，问题在哪我不清楚 但是可能包括 但不限于 有 bug 某些我觉得可能影响不大 但是实际却影响很大的区别。因此最好的办法就是把所有的区别 包括潜在的 bug 之类 不等价的问题 按照可能性和优先级的顺序列出来，然后一个一个做消融实验就能定位到问题了。这里 我不是让你直接去做消融实验，你要做的事情是帮我过一遍代码，按照可能性和优先级的顺序列出来需要做消融实验的疑点。并且把具体的的消融方案和计划写出来，我会让同事去做你写的每一个消融实验。请你注意，我感觉LazyEviction是对的，因此LazyEviction是作为参照，不要有任何的消融实验去改LazyEviction的代码，我们是要去改 R-KV 的代码，看看把哪些东西修了 结果就很接近了。请你把你写的方案详细的记录到一个 md 文件里面 放在R-KV/docs的底下。


## 新指令概要
- 参考 `R-KV/docs/speckv_qwen` 的文档/规范，建立同类记录体系（本目录）。
- 额外约束：不得影响现有算法/实验；所有消融开关必须 cfg 可配且默认旧行为；必要时代码隔离。
- 消融启动脚本集中放置于 `R-KV/weian_script/ablations_fullkv/`。
- 长跑实验由用户执行；Agent 交付前需跑短 Smoke。

## 已继承的规范
- 记录方式、停机告警条款、风格等同 `R-KV/docs/speckv_qwen/*`。

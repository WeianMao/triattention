以下内容为本项目的历史消息留档（供后续查阅，避免遗漏要求）。

## 用户原始指令（转存）

> “我最早模拟了一个kv 压缩算法，模拟的脚本是weian_development/online_k_pruning_viz/attention_pruning_case_study_hybrid_rounds_xtrace.py，对应的模型是 qwen。然后我同事尝试在LazyEviction 底下测试了这个算法，入口是LazyEviction/weian_script/run_sparse_prefill_keep_sharded_eval.sh，数学推理结果挺好的。然后，又有一个同事在R-KV 下面重构了这个算法，入口是R-KV/weian_script/aime24_official_sampled8/run_speckv_aime24_official_sampled8.sh。这个过程进行了众多的适配，比如适配 llama 模型，以R-KV 的 style 重构了代码。 这次我希望你帮我在RKV的框架下将我的SpecKV算法适配qwen模型。 然后你适配完qwen模型之后,要比较的有两种setting,所以你需要给我两个脚本分别对应着两个setting. 这两个脚本分别是R-KV/weian_script/aime24_official_sampled8/run_rkv_aime25_official_sampled8_qwen.sh 和R-KV/weian_script/aime24_official_sampled8/run_speckv_aime24_official_sampled8.sh。 这里有一个比较复杂的背景以及特殊的要求,我在这里给你提一下。背景就是根据我之前测试的结果来看,以下这两个实验的性能是很好的。attention_pruning_case_study_hybrid_rounds_xtrace.py 和LazyEviction/weian_script/run_sparse_prefill_keep_sharded_eval.sh。 并且这两个是最早实现的两个版本 然后呢后来我又实现了run_speckv_aime24_official_sampled8.sh这个版本 但是这个版本的效果呢就很差。 那么为什么差的原因目前尚不明了 有可能是我在重构我算法的代码的时候 引入了一些bug或者是一些不合适的setting 也有一种可能是因为这个脚本用的是 llama模型 和之前 qwen模型不是同一个 因此有可能就是我的算法 在 llama这个模型上它performance就是不好。 所以,这个run_speckv_aime24_official_sampled8.sh版本对应的算法实现不一定是全对的，但是也不一定肯定就有问题。 那么这样的话呢就引出了额外和你任务相关的两个问题， 第一,因为你是在RKV框架下重构我算法的代码,因此你在实现的时候还是要很大程度上参考run_speckv_aime24_official_sampled8.sh这个版本。 但是另一方面,因为这个版本不一定是全对的, 因此你在实现算法的时候,还是要参考我之前的attention_pruning_case_study_hybrid_rounds_xtrace.py 和run_sparse_prefill_keep_sharded_eval.sh 这两个版本。 并且你要确保你重构的代码在算法逻辑上和之前的这两个版本是等价的,不能有问题。 刚刚讲的这些就是你要注意的第一点 那么现在还有另外一点你要注意 就我前面讲的,理想情下 这三个版本应该是完全等价的 因此就不会出现什么问题 你就可以同时参考三个版本去重构你的代码 甚至直接复用run_speckv_aime24_official_sampled8.sh这个版本的代码。 但是呢,现在的一个问题是有可能这个版本的代码是有问题的,和之前两个版本是不一样的。 那么如果你发现有这种情况的话,你要做的事情是立马停下来告诉我发生了什么问题,哪个地方关键的算法逻辑是对不上的。 你要警告我这个警告的优先级甚至比你实现代码的优先级还高。 OK,这些就是你要注意的问题 最后呢,我给你一个Tips 这样子你就能更好地去实现你的任务 那么现在的情况就是 RKV中的这个版本（run_speckv_aime24_official_sampled8.sh） 它用的是RKV的代码Style 和规范去实现的 但是它的模型用的是Llama模型；之前这个LazyEviction的这里面这个版本（run_sparse_prefill_keep_sharded_eval.sh）它用的代码Style和RKV是不一样 它用的是自己的Style 但是它的模型是千问模型 那么你现在要实现的这个版本 等于是在RKV的框架下 用RKV的开发范式跟Style 去适配千问模型 因此关于代码实现的方式 你是可以参考我已经在RKV框架下实现的这个脚本的方式去实现 并且确保你所有的代码都在RKV的这个文件夹下 但是关于适配千问模型的关键Detail 你可以去参考我的LazyEviction里面的实现 这样子是最方便的方法。”

## 额外备注
- 本项目不使用多 Agent 协作，进度/记录集中在 `project_todo.md` 与 `status_overview.md`。
- 后续新增要求或发现问题，追加在此文件末尾（保持原文，不要覆盖）。***
